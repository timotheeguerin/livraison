{
  "name": "@livraison/installers",
  "version": "0.0.4",
  "private": true,
  "scripts": {
    "build": "pnpm build:shell",
    "livraison": "cargo run --package livraison",
    "build:shell": "pnpm livraison script --target sh --name Livraison --download-url 'https://github.com/timotheeguerin/livraison/releases/download/{version}/{filename}' --out install.sh --bin-name livraison"
  }
}
