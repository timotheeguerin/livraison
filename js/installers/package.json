{
  "name": "@livraison/installers",
  "version": "0.0.4",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm build:shell && pnpm build:pwsh",
    "build:pwsh": "pnpm livraison script --target pwsh --name Livraison --download-url \"https://github.com/timotheeguerin/livraison/releases/download/{version}/{filename}\" --latest-download-url \"https://github.com/timotheeguerin/livraison/releases/latest/download/{filename}\" --out install.ps1 --bin-name livraison",
    "build:shell": "pnpm livraison script --target sh --name Livraison --download-url \"https://github.com/timotheeguerin/livraison/releases/download/{version}/{filename}\" --latest-download-url \"https://github.com/timotheeguerin/livraison/releases/latest/download/{filename}\" --out install.sh --bin-name livraison",
    "livraison": "cargo run --package livraison",
    "test:bash": "tsx test/docker-test.ts --shell bash",
    "test:zsh": "tsx test/docker-test.ts --shell zsh"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "execa": "catalog:",
    "picocolors": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
